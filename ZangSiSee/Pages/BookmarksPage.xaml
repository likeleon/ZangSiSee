<?xml version="1.0" encoding="utf-8" ?>
<pages:BookmarksXaml xmlns="http://xamarin.com/schemas/2014/forms"
                     xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                     xmlns:pages="clr-namespace:ZangSiSee.Pages"
                     x:Class="ZangSiSee.Pages.BookmarksPage"
                     Icon="Transparent"
                     Title="{Binding Title}">
  <ListView x:Name="list"
            HasUnevenRows="True"
            SeparatorColor="{StaticResource grayPrimary}"
            ItemsSource="{Binding Bookmarks}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <ViewCell.View>
            <Grid Padding="15">
              <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
              </Grid.RowDefinitions>
              <StackLayout Spacing="10" Orientation="Horizontal">
                <Label Text="{Binding Bookmark.BookTitle}" />
                <Label Text="{Binding Bookmark.PageNumber, StringFormat='{}{0} 페이지'}" Style="{StaticResource sub}" />
              </StackLayout>
              <Label Grid.Row="1" Text="{Binding Bookmark.CreationTime, StringFormat='{}{0:f}'}" Style="{StaticResource sub}" />
            </Grid>
          </ViewCell.View>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</pages:BookmarksXaml>